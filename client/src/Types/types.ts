export type Plan = {
  name: string;
  price: string;
  monthlyPrice: string;
  discount: string;
  description: string;
  duration: string;
}

export type User = {
  firstName: string;
  lastName: string;
  email: string;
  phoneNumber: string;
  userSignature: string;
  price: string;
  duration: string;
}